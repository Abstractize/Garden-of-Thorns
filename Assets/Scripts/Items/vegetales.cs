using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class vegetales : Items
{
    // Start is called before the first frame update
    void Start()
    {
        t = typeI.vegetales;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
