using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tomate_cortado : Items
{
    // Start is called before the first frame update
    void Start()
    {
        t = typeI.tomate_cortado;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
