using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class queso : Items
{
    // Start is called before the first frame update
    void Start()
    {
        t = typeI.queso
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
