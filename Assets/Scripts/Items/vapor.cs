using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class vapor : Items
{
    // Start is called before the first frame update
    void Start()
    {
        t = typeI.vapor;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
