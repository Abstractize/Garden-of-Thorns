using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pizza_quemada : Items
{
    // Start is called before the first frame update
    void Start()
    {
        t = typeI.pizza_quemada;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
