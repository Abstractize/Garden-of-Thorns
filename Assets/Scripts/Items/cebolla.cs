using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cebolla : Items
{
    // Start is called before the first frame update
    void Start()
    {
        t = typeI.cebolla;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
