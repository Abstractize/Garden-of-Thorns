using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class awa : Items
{
    // Start is called before the first frame update
    void Start()
    {
        t = typeI.awa;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
