using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class lechuga : Items
{
    // Start is called before the first frame update
    void Start()
    {
        t = typeI.lechuga;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
