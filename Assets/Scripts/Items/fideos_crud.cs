using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fideos_crud : Items
{
    // Start is called before the first frame update
    void Start()
    {
        t = typeI.fideos_crud;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
