using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class salami : Items
{
    // Start is called before the first frame update
    void Start()
    {
        t = typeI.salami;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
